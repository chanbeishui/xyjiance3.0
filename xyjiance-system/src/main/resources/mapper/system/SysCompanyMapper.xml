<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyjiance.system.mapper.SysCompanyMapper">

    <resultMap type="SysCompany" id="SysCompanyResult">
        <id     property="companyId"   column="company_id"    />
        <result property="parentId"    column="parent_id"     />
        <result property="ancestors"   column="ancestors"     />
        <result property="companyName" column="company_name"  />
        <result property="orderNum"    column="order_num"     />
        <result property="leader"      column="leader"        />
        <result property="phone"       column="phone"         />
        <result property="email"       column="email"         />
        <result property="status"      column="status"        />
        <result property="delFlag"     column="del_flag"      />
        <result property="createBy"    column="create_by"     />
        <result property="createTime"  column="create_time"   />
        <result property="updateBy"    column="update_by"     />
        <result property="updateTime"  column="update_time"   />
    </resultMap>

    <sql id="selectCompanyVo">
        select c.company_id, c.parent_id, c.ancestors, c.company_name, c.order_num, c.leader, c.phone, c.email,
               c.status, c.del_flag, c.create_by, c.create_time, c.update_by, c.update_time
        from sys_company c
    </sql>

    <select id="selectCompanyList" parameterType="SysCompany" resultMap="SysCompanyResult">
        <include refid="selectCompanyVo"/>
        where c.del_flag = '0'
        <if test="companyName != null and companyName != ''">
            AND c.company_name like concat('%', #{companyName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND c.status = #{status}
        </if>
        order by c.parent_id, c.order_num
    </select>

    <select id="selectCompanyById" parameterType="Long" resultMap="SysCompanyResult">
        <include refid="selectCompanyVo"/>
        where c.company_id = #{companyId}
    </select>

    <select id="checkCompanyNameUnique" resultMap="SysCompanyResult">
        <include refid="selectCompanyVo"/>
        where c.company_name = #{companyName} and c.parent_id = #{parentId} and c.del_flag = '0' limit 1
    </select>

    <select id="hasChildByCompanyId" parameterType="Long" resultType="int">
        select count(1) from sys_company where parent_id = #{companyId} and del_flag = '0'
    </select>

    <select id="selectChildrenCompanyById" parameterType="Long" resultMap="SysCompanyResult">
        select * from sys_company where find_in_set(#{companyId}, ancestors) and del_flag = '0'
    </select>

    <insert id="insertCompany" parameterType="SysCompany" useGeneratedKeys="true" keyProperty="companyId">
        insert into sys_company(
            <if test="parentId != null and parentId != 0">parent_id,</if>
            <if test="ancestors != null and ancestors != ''">ancestors,</if>
            company_name,
            <if test="orderNum != null">order_num,</if>
            <if test="leader != null and leader != ''">leader,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="email != null and email != ''">email,</if>
            <if test="status != null">status,</if>
            create_by, create_time
        ) values (
            <if test="parentId != null and parentId != 0">#{parentId},</if>
            <if test="ancestors != null and ancestors != ''">#{ancestors},</if>
            #{companyName},
            <if test="orderNum != null">#{orderNum},</if>
            <if test="leader != null and leader != ''">#{leader},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="email != null and email != ''">#{email},</if>
            <if test="status != null">#{status},</if>
            #{createBy}, sysdate()
        )
    </insert>

    <update id="updateCompany" parameterType="SysCompany">
        update sys_company
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="ancestors != null and ancestors != ''">ancestors = #{ancestors},</if>
            <if test="companyName != null and companyName != ''">company_name = #{companyName},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="leader != null">leader = #{leader},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            update_by = #{updateBy}, update_time = sysdate()
        </set>
        where company_id = #{companyId}
    </update>

    <delete id="deleteCompanyById" parameterType="Long">
        update sys_company set del_flag = '2' where company_id = #{companyId}
    </delete>

</mapper>
